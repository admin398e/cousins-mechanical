# Cousins Mechanical Services
User-agent: *
Allow: /

# Staff-only areas — never index these
Disallow: /admin
Disallow: /admin.html
Disallow: /driver
Disallow: /driver.html

# API responses are not pages
Disallow: /api
Disallow: /api/
Disallow: /ukvd
Disallow: /v1/
Disallow: /bookings
Disallow: /messages
Disallow: /track
Disallow: /notify
Disallow: /service-requests

# Un-hydrated template tokens: the pages are React-rendered, so a crawler that
# reads the raw HTML before JS runs can follow a literal "{{ token }}" as a URL.
# These are never real pages. (Seen in Search Console as /{{ waConfirm }}.)
Disallow: /*{{
Disallow: /*%7B%7B

# The error page is NOT blocked here on purpose. /404 answers with a real 404
# status now, and a 404 drops out of the index by itself; blocking it would
# only stop Google seeing the status that tells it to.

# Internal pricing worksheets
Disallow: /pricingworkout.html
Disallow: /tyre_finder.html
Disallow: /ctyres_catalogue.html

# Block the AI scrapers that ignore commercial value but keep Google/Bing
User-agent: GPTBot
Disallow: /

User-agent: CCBot
Disallow: /

Sitemap: https://cousinsmechanicalservices.co.uk/sitemap.xml
